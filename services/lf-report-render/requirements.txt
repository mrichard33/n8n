# Pinned to the exact versions the test suite and the visual-regression
# baseline were validated against. matplotlib wheels vendor FreeType, so the
# chart rasterises identically wherever the same wheel runs — bumping
# matplotlib (or reportlab) can shift pixels and requires a baseline re-bless
# recorded in docs/REPORT_SPEC.md (CI enforces the pairing).
fastapi==0.141.1
uvicorn==0.52.3
reportlab==5.0.0
matplotlib==3.11.1
pillow==12.3.0
numpy==2.4.6
pypdf==6.16.1
pydantic==2.13.4
