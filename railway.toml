[deploy]
startCommand = 'sh -c "rm -rf /n8n/volume/.n8n/nodes/node_modules/n8n-nodes-base 2>/dev/null; /usr/local/bin/fix-collation.sh || true; exec n8n start"'
healthcheckPath = "/healthz"
healthcheckTimeout = 600
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 8
